apiVersion: v1
kind: Pod
metadata:
  name: dind-pod-aks-remount
spec:
  hostUsers: false
  containers:
    - name: docker
      image: snehapark4appssacr.azurecr.io/dind:v10
      args: 
        - "https://github.com/docker/compose-for-agents.git"
        - "compose-for-agents/langchaingo"
      securityContext:
        capabilities:
          add: ["SYS_ADMIN", "NET_ADMIN"]
      volumeMounts:
        - mountPath: /var/lib/docker
          name: docker-volume
      resources:
        requests: { cpu: 500m, memory: 1Gi }
        limits: { cpu: 4000m, memory: 8Gi, ephemeral-storage: 40Gi }
  volumes:
    - name: docker-volume
      emptyDir: {}
